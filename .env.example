# Exemple uniquement. Ne pas stocker un vrai secret dans ce fichier.
APP_ENV=home
PRUEFVERSAND_CONFIG=config/home.example.toml
MYSQL_PASSWORD=change-me-local-only
MYSQL_ROOT_PASSWORD=change-root-local-only

